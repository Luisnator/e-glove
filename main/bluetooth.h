#ifndef BLUETOOTH_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define BLUETOOTH_H

void setupBluetooth();
void testfun();
void sendInt(int val);

#endif